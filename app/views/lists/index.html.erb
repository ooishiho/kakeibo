<h1>一覧</h1>
<% @lists.each do |list| %>

<%= list.day %>
<%= list.title %>
<%= list.amount %>


<span>
 <%= link_to '詳細へ', list_path(list.id) %>
</span>

<% end %>

<p>  <%= link_to '新規投稿', new_list_path %></p>