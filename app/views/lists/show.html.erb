<p>投稿詳細</p>
<p><%= @list.month %></p>
<p><%= @list.day %></p>
<p><%= @list.title %></p>
<p><%= @list.amount %></p>

<%= link_to "変更する", edit_list_path(@list.id) %>|
<%= link_to "削除する", list_path(@list.id), method: :delete,"data-confirm" => "削除しますか？" %> |
<%= link_to "戻る", lists_path %>